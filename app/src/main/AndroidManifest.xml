<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pisquare.iskcon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MangalaActivity" />
        <activity android:name=".DarshanaActivity" />
        <activity android:name=".GuruAstakaActivity" />
        <activity android:name=".TulsiActivity" />
        <activity android:name=".NarasimhaActivity" />
        <activity android:name=".NkavacaActivity" />
        <activity android:name=".GovindamActivity" />
        <activity android:name=".SavaranaActivity" />
        <activity android:name=".NamaActivity" />
        <activity android:name=".NaraAActivity" />
        <activity android:name=".SriVenkateshaActivity" />
        <activity android:name=".SriVandanaActivity" />
        <activity android:name=".INDRA"></activity>
    </application>

</manifest>